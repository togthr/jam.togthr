{
  "name": "jam-togthr",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "fmt": "npx prettier --write .",
    "fmt:check": "npx prettier --check .",
    "lint:md": "npx markdownlint-cli2 '**/*.md'",
    "verify": "npm run fmt:check && npm run lint:md"
  },
  "devDependencies": {
    "markdownlint-cli2": "^0.13.0",
    "prettier": "^3.3.3"
  }
}
